import React from 'react';
import './LoginSubmitBtn.scss';

const LoginSubmitBtn = props => {
  return (
    <div className="loginSubmitBtn">
      <h1>LoginButton Temp</h1>
      {props.value}
    </div>
  );
};

export default LoginSubmitBtn;
